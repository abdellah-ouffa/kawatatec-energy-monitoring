venv/
# Python automaticallt generates this directory to store compiled bytecode versions of python files (.pyc files)
# These are generated to make python execution faster ,but they are not needed in version control because,they 
# are specific to local machine and can be regenerated 
__pycache__/
# These are python compiled files ,generated by python to speed up execution by skippig the parsing stage 
*.pyc
# this directory contains all the dependencies (JavaScript packages) installed via npm or yarn for the frontend
node_modules/